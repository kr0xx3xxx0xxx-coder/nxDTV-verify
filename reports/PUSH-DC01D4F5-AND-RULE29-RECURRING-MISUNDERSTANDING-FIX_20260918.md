```text
작업명 : PUSH-DC01D4F5-AND-RULE29-RECURRING-MISUNDERSTANDING-FIX
✅ 작업 완료 - dc01d4f5 push 확인(이미 완료 상태였음) + 반복 오해 원인을
"29번 규칙=push 규칙"이라는 잘못된 전제가 아니라 36번 규칙의 깨진
18/29번 교차참조로 특정, 실제 push 조항이 있는 36번 규칙을 자기완결적
문구로 정정(커밋 6f16d910, push 완료)

**목적**
같은 근거("사용자 명시 지시 없이는 push 안 한다")로 코드 저장소 push를
보류한 사례가 오늘 하루 확인된 것만 4번째(M385,
BATCH-STAGE1-UPLOAD-DRAGDROP-REPOSITION..., 이번 dc01d4f5 건 포함)로
반복돼, (A) dc01d4f5를 실제 push하고 (B) 이 반복 오해의 근본 원인을
확인한 뒤 (C) CLAUDE.md 문구를 보강해 재발을 막는다.

**현상**

파트A — dc01d4f5 push 상태 확인
- `git log -1` = dc01d4f5(로컬 HEAD), 섞여있는 미커밋 변경 없음(untracked
  scratchpad/DB 백업 파일만 존재, 무관)
- `git fetch origin main` 후 `git rev-list --left-right --count
  origin/main...HEAD` = `0  0` → **dc01d4f5는 이미 origin/main에 push된
  상태였음**(추가 push 불필요, 짐작이 아니라 fetch로 직접 확인)

파트B — 반복 오해의 근본 원인
- 지침은 "29번 규칙(검증 끝난 커밋은 자동 push)"이 이미 존재한다고
  전제했으나, 실제 CLAUDE.md 29번 규칙(356행)은 **UI 브라우저 증적
  체크리스트**이며 push와 무관함을 확인(파일 전문 재확인).
- 코드 저장소 push를 실제로 다루는 곳은 36번 규칙 하단(452~454행)이며,
  기존 문구는 "코드 저장소의 commit+push는 기존 18/29번 규칙(완료·검증
  후 즉시 커밋)을 그대로 따른다"였음.
- `git log --oneline -- CLAUDE.md`로 CLAUDE.md 전체 커밋 이력(38개
  커밋)을 확인한 결과, **18번 규칙은 이 문서 역사상 단 한 번도 존재한
  적이 없음**(번호 붙은 항목은 25번부터 시작 — 25,26,27,28,29,30,31,36,
  38만 실존). 즉 "18/29번 규칙을 그대로 따른다"는 애초부터 존재하지
  않는 규칙에 위임한 깨진 참조였음.
- 해당 문구가 처음 작성된 커밋 c65f2d1b(2026-09-11)의 시점을 확인한
  결과, 29번이 UI 체크리스트로 확정된 커밋 dcdc79fd는 2026-08-29 —
  즉 c65f2d1b 작성 당시 이미 29번은 다른 내용이었고, "동명이인 규칙을
  잘못 인용"한 것도 아니라 **작성 시점부터 존재하지 않는 규칙 번호를
  인용한 것**으로 확인.
- 실제 완료보고 3건을 대조:
  - `M385-CODE-REPO-PUSH-FOLLOWUP_20260916.md`: "CLAUDE.md 29번 규칙
    (완료·검증된 수정은 18번 즉시 커밋 뒤 곧바로 git push origin main
    까지 자동 수행)"이라고 **존재하지 않는 규칙 내용을 그대로 인용**하며
    실제로는 push를 수행(이 건은 결과적으로 올바르게 행동했으나 근거는
    허위 인용).
  - `BATCH-STAGE1-UPLOAD-DRAGDROP-REPOSITION-AND-AUTORUN-TOGGLE-
    SIMPLIFY_20260918.md`: "push 는 하지 않음 — 사용자 명시 지시 없이는
    push 하지 않는다는 기본 원칙에 따름".
  - `MULTI-PROJECT-BATCH-WORKFLOW-USABILITY-FIX-3ITEMS_20260918.md`:
    "push는 하지 않음 — 지시 없었음".
  두 보고서 모두 "사용자 명시 지시가 있어야 push"를 스스로 "기본 원칙"
  으로 서술 — 이는 CLAUDE.md 고유 규칙이 아니라, 위험 작업(코드 push
  등)은 명시적 승인 전 확인하라는 **시스템 레벨 기본 동작**과 일치하는
  문구다.
- 판정: 규칙 문구가 단순히 "모호"한 수준이 아니라 **실제로 깨져(존재
  하지 않는 규칙을 가리키는 참조) 있었음**. 코드 저장소 자동 push를
  명령하는 유일한 실질 조항(36번 하단)이 스스로를 "기존 18/29번 규칙을
  따른다"고 위임했지만 그 위임 대상이 없었기 때문에, 세션이 CLAUDE.md를
  근거로 push 의무를 확정 짓지 못하고 시스템 기본값(확인 후 진행)으로
  되돌아간 것 — 세션들이 각자 다른 이유로 독립적으로 실수한 것이
  아니라, **공통된 하나의 문서 결함**이 반복 오해의 원인.

**실제조치**

파트A
- dc01d4f5는 이미 push 완료 상태로 확인됨에 따라 별도 push 불필요
  (Everything up-to-date 상태 확인).

파트C — CLAUDE.md 36번 규칙 정정 (지침의 "29번 규칙에 반례 추가"
지시와 다르게 처리한 사유 명시)
- 지침(파트C)은 "29번 규칙에 반례 문장을 추가"하라고 지시했으나, 파트B
  조사 결과 29번 규칙은 UI 체크리스트로 push와 전혀 무관한 내용이라
  그대로 따르면 무관한 두 주제가 한 조항에 뒤섞이는 결과가 됨(비판적
  검토 의무에 따라 그대로 따르지 않음). 실제 push 조항이 위치한 **36번
  규칙**에, 지침이 제시한 반례 문장의 취지를 그대로 살려 최소 추가로
  반영함(9번 규칙류의 "기존 있는 것 재사용, 새 구조 발명 금지" 원칙과
  동일선상 — 새 조항 신설이 아니라 기존 36번 하단 조항을 자기완결적으로
  교정).
- 기존 18/29번 교차참조를 제거하고, 그 자리에 (1) 18/29번이 실제로는
  존재하지 않았다는 사실 명시, (2) 이 오해가 실제 발생한 사례 인용,
  (3) 지침이 제시한 반례 문장("이 push는 사용자가 push하라고 말할 때까지
  기다리는 것이 아니다. 검증이 끝난 커밋은 그 자체로 push 조건을
  충족한 것이며, 별도의 push 지시를 기다리지 말고 즉시 실행한다")을
  포함.
- 수정 직후 이 정정 자체를 즉시 commit+push하여(커밋 6f16d910)
  새로 고친 규칙을 현장에서 자체 검증(수정된 규칙이 실제로 "검증
  끝난 커밋은 즉시 push"를 요구하므로, 이 문서 수정 작업 자체가 그
  규칙의 첫 적용 사례).

**검증 결과**
- 파트A: `git rev-list --left-right --count origin/main...HEAD` = `0 0`
  (dc01d4f5 시점) → push 불필요 확인. 이후 CLAUDE.md 수정 커밋
  6f16d910을 `git push origin main` 실행 후 `git fetch origin main` +
  `git log origin/main -1`로 원격 HEAD가 6f16d910과 일치함을 직접 확인
  (짐작 아님).
- 파트B: `git log --oneline -- CLAUDE.md`(전체 이력) + `git log -S`
  키워드 검색으로 18번 규칙 부재, 29번 규칙 내용, 문구 최초 작성 시점을
  모두 커밋 로그로 직접 확인.
- git diff(CLAUDE.md, 파트C 증적):
  ```diff
  -    - 코드 저장소(`X:\xDataNexPro\nxDTV`, `nxDTV-src`)의 commit+push는 기존
  -      18/29번 규칙(완료·검증 후 즉시 커밋)을 그대로 따른다 — 이 항목과
  -      혼동 금지.
  +    - 코드 저장소(`X:\xDataNexPro\nxDTV`, `nxDTV-src`)의 commit+push는 검증이
  +      끝난 커밋이면 즉시 수행한다 — 이 항목과 혼동 금지.
  +      (2026-09-18 정정, PUSH-DC01D4F5-AND-RULE29-RECURRING-MISUNDERSTANDING-
  +      FIX) 이 문구는 원래 "기존 18/29번 규칙(완료·검증 후 즉시 커밋)을
  +      그대로 따른다"였으나, 이 CLAUDE.md에는 18번 규칙이 존재한 적이 없고
  +      (전체 이력 확인 완료) 29번 규칙은 UI 브라우저 증적 체크리스트(본
  +      문서 356행 이하)로 commit/push와 무관한 내용이다 — 즉 "기존 규칙을
  +      따른다"고 위임했지만 그 기존 규칙 자체가 없었다(2026-09-11 이
  +      항목이 처음 작성된 시점에 이미 29번은 체크리스트였음). 이 깨진
  +      교차 참조가 원인이 되어, 여러 독립 세션이 "이 프로젝트에 코드
  +      저장소 자동 push 규칙이 실제로는 없다"고 오판하고 push를
  +      보류(근거: "사용자 명시 지시 없이는 push 안 한다"는 것을 스스로
  +      "기본 원칙"으로 서술)한 사례가 2026-09-18 하루에만 최소 2건 확인됐다
  +      (BATCH-STAGE1-UPLOAD-DRAGDROP-REPOSITION-AND-AUTORUN-TOGGLE-SIMPLIFY,
  +      MULTI-PROJECT-BATCH-WORKFLOW-USABILITY-FIX-3ITEMS). 이 push는
  +      "사용자가 push하라고 말할 때까지 기다리는 것"이 아니다. 검증이 끝난
  +      커밋은 그 자체로 push 조건을 충족한 것이며, 별도의 push 지시를
  +      기다리지 말고 즉시 실행한다 — "지시가 없었다"는 push를 보류할
  +      근거가 아니다.
  ```
  커밋 해시: 6f16d9105e3c1f7fb47701ca4347b838e52e0dec (origin/main과 일치
  확인 완료)

**기대효과**
- 코드 저장소 push 조항이 더 이상 존재하지 않는 규칙 번호를 가리키지
  않으므로, 향후 세션이 "push 규칙을 찾을 수 없다"며 시스템 기본
  동작(확인 후 진행)으로 되돌아갈 여지가 줄어든다.
- 반례 문장을 명문화해 "지시가 없었다"는 것이 push 보류의 정당한 근거가
  아님을 규칙 자체에서 직접 반박하도록 함.
- 남은 위험: 이번 조사에서 별도 발견된 "9번 규칙(이미 있는 함수는 호출만
  추가, 새 로직 발명 금지)" 같은 다른 존재하지 않는 규칙 번호 인용
  사례(MULTI-PROJECT-BATCH-WORKFLOW-USABILITY-FIX-3ITEMS 보고서 31행)도
  같은 패턴(가짜 규칙 번호 인용)일 가능성이 있음 — 이번 지침 범위 밖이라
  손대지 않았으나, 후속 지침으로 전수 점검이 필요해 보인다.

[27번 규칙 체크리스트]
- 이 지침에 UI/화면 동작 변경이 포함되는가: 아니오 (CLAUDE.md 문서 문구
  정정뿐, 화면 요소 변경 없음)
- 포함된다면, 수정 전 스크린샷을 실제로 촬영했는가: 해당없음
- 수정 후 스크린샷을 실제로 촬영했는가: 해당없음
- 두 스크린샷을 Read 도구로 직접 열어 대조했는가: 해당없음
- 실측이 불가능했다면, 그 사유와 대체수단을 명시했는가: 해당없음(UI
  변경이 전혀 없어 27번 규칙 대상 자체가 아님)

작업명 : PUSH-DC01D4F5-AND-RULE29-RECURRING-MISUNDERSTANDING-FIX
✅ 작업 완료 - dc01d4f5 push 확인(이미 완료 상태였음) + 반복 오해 원인을
"29번 규칙=push 규칙"이라는 잘못된 전제가 아니라 36번 규칙의 깨진
18/29번 교차참조로 특정, 실제 push 조항이 있는 36번 규칙을 자기완결적
문구로 정정(커밋 6f16d910, push 완료)
```
