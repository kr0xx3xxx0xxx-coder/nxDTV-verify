```text
작업명 : SERVER-RESTART-2026-09-18
✅ 작업 완료 - web_server.py(포트 8000 운영 서버) 재기동 및 포트 리스닝 확인

**목적**
사용자 요청 "서버 재 시작 해줘"에 따라 운영 서버(포트 8000, web_server.py)를 재기동한다.
코드 변경은 포함되지 않은 순수 운영 조작 작업이다.

**현상**
- netstat -ano | grep ':8000' | grep LISTENING → 기존 PID 17596(python.exe,
  X:\xDataNexPro\nxDTV\web_server.py, 기동시각 2026-09-18 07:02:58)이 포트 8000을
  점유 중이었음.

**실제조치**
1. taskkill /PID 17596 /F 로 기존 서버 프로세스 종료
2. netstat 재확인 → 8000번 포트 LISTENING 항목 없음(정상 해제) 확인
3. `cd X:\xDataNexPro\nxDTV && python web_server.py` 를 백그라운드(nohup)로 재기동
   - web_server.py 내부 자체 로직에 따라 "분리 기동된 서버 PID: 17696" 으로
     새 프로세스가 자체적으로 재기동되어 실제 리스닝 프로세스는 PID 17696
   - 바인딩 host/port는 환경변수 미지정 → 기본값 127.0.0.1:8000 그대로 사용

**검증 결과**
- netstat -ano | grep ':8000' | grep LISTENING
  → TCP 127.0.0.1:8000  LISTENING  PID 17696 확인
- 서버 로그(logs/server_restart_20260918.log): "분리 기동된 서버 PID: 17696"
- curl 헬스체크(HTTP 200 등)는 이번 세션 Bash 권한 정책상 거부되어 미실시
  (포트 리스닝 확인 + 서버 자체 기동 로그로 정상 기동 확인 — 실측 대체수단이며
  사유는 세션 권한 정책상 curl 호출 차단)

**기대효과**
포트 8000 운영 서버가 최신 프로세스로 재기동되어, 이전 프로세스에 남아있을 수 있는
메모리/커넥션 상태가 초기화된 상태로 서비스가 재개된다.

[27번 규칙 체크리스트]
- 이 지침에 UI/화면 동작 변경이 포함되는가: 아니오 (서버 프로세스 재기동만 수행,
  코드 수정 없음)
- 포함된다면, 수정 전 스크린샷을 실제로 촬영했는가: 해당없음
- 수정 후 스크린샷을 실제로 촬영했는가: 해당없음
- 두 스크린샷을 Read 도구로 직접 열어 대조했는가: 해당없음
- 실측이 불가능했다면, 그 사유와 대체수단을 명시했는가: 해당없음 (UI 변경 없음)

작업명 : SERVER-RESTART-2026-09-18
✅ 작업 완료 - web_server.py(포트 8000 운영 서버) 재기동 및 포트 리스닝 확인
```
